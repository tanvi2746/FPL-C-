#include <stdio.h>

void main() 
{
    char ch;
    printf("Enter the alphabet: ");
    scanf("%c",&ch);
   if ( ch=='a' || ch=='e' || ch=='i' || ch=='o' || c=='u') 
        printf("%c is a vowel.\n", ch);
   else 
        printf("%c is not a vowel.\n", ch);
}
/* Enter the alphabet: u
u is a vowel. */
